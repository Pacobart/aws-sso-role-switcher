name: aws-sso-role-switcher
run-name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: python:3.9
    steps:
      - run: echo "building package"
  test:
    runs-on: ubuntu-latest
    container:
      image: python3.9
    steps:
      - run: echo "testing code"
      - run: make test